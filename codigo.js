
        function saludar(){
            var user = document.getElementById("txtUsername").value;
            var pass = document.getElementById("txtPassword").value;
            alert("Te damos la bienvenida " + user);
            console.log("tu password no es correcto" + pass);
        }
        
        function sumar(){
            var n1 = parseInt(document.getElementById("txtUsername").value);
            var n2 = parseInt(document.getElementById("txtPassword").value);
            alert("La suma es:  " + (n1 + n2));
        }

   